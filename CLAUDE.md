# StrongLifts App

## Quality Gate

All features and changes must achieve **100% test coverage** (statements, branches, lines, functions) before they can be marked as complete. Run the full test suite and verify coverage before finishing any task.

## Formatting

**Always run `npm run format` after making code changes.** This uses Prettier with the Svelte plugin to ensure consistent formatting across all source files.
